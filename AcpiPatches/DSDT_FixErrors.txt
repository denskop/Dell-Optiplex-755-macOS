# Fix Or operation
into_all all code_regex Or\s\(Zero,\sLocal0\) replaceall_matched
begin
Or (Local0, Zero, Local0)
end;

# Fix return type
into method label _CRS parent_label TPM code_regex Return\s\(Zero\) replaceall_matched
begin
Return (Buffer(){Zero})
end;

# Fix PCI Root _UID
into device label PCI0 code_regex Name\s\(_UID,\s0x04\) replace_matched begin Name (_UID, Zero) end;